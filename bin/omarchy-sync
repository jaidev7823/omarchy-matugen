#!/bin/bash
# 1. Resolve the path
REAL_PATH=$(readlink -f /home/jaidev/.config/omarchy/current/background)

# 2. Update swww (assuming daemon is running)
swww img "$REAL_PATH" --transition-type grow

# 3. Trigger Matugen
matugen image "$REAL_PATH"
